services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dashboards
    ports:
      - '5433:5432'
    volumes:
      - dashboards_pgdata:/var/lib/postgresql/data

volumes:
  dashboards_pgdata:
